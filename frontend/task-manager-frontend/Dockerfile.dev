# Use the Node.js image for development
FROM node:18-slim
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy the rest of the source code (this will be overridden by volume mounting)
COPY . .

# Expose the port used by the Vite dev server (default is 3000)
EXPOSE 3000

# Run the development server 
CMD ["npm", "run", "dev", "--", "--host"]